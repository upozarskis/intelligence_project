services:
  api:
    build: .
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
  frontend:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./frontend:/usr/share/nginx/html
