version: '3.8'

services:
  api:
    build: .
    restart: always
    environment:
      DB_URL: ${DB_URL}
      SECRET_KEY: ${SECRET_KEY}
      ALGORITHM: ${ALGORITHM}
      NEWS_API_URL: ${NEWS_API_URL}   
      NEWS_API_KEY: ${NEWS_API_KEY}   
    ports:
      - "8000:8000"